<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.admin.collegenoti">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".api.IPActivity" />
        <activity android:name=".activities.UserRegisterActivity" />
        <activity android:name=".activities.UserListActivity" />
        <activity android:name=".activities.AdminHomeActivity" />
        <activity android:name=".activities.PreRigesterActivity" />
        <activity android:name=".activities.StudentRegisterActivity" />
        <activity android:name=".activities.LecturersActivity" />
        <activity android:name=".activities.AdminActivity" />
        <activity android:name=".activities.PrincipalActivity" />
        <activity android:name=".activities.ParentsActivity" />
        <activity android:name=".activities.StudentsActivity" />
        <activity android:name=".activities.UserLoginActivity" />
        <activity android:name=".activities.StudentsResultDetailsActivity" />
        <activity android:name=".activities.AttendanceActivity" />
        <activity android:name=".activities.TimetableActivity" />
        <activity android:name=".activities.ShortageAtActivity" />
        <activity android:name=".activities.FeesActivity" />
        <activity android:name=".activities.EventsActivity" />
        <activity android:name=".activities.ImagesActivity" />
        <activity android:name=".activities.HolidayActivity" />
        <activity android:name=".activities.LuploadsActivity"></activity>
    </application>

</manifest>